Create EGN by given data (stored in a wordlist) or vice versa.

```
 python3 main.py
```
! warning !
` Before starting main.py, you must create a folder named "wordlists" where wordlists are going to be stored `
